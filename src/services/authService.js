export async function signInWithEmail() {
  return { ok: true, message: 'Mock sign-in only. Firebase Auth is not connected yet.' }
}

export async function signInWithGoogle() {
  return { ok: true, message: 'Mock Google sign-in only. Firebase Auth is not connected yet.' }
}
